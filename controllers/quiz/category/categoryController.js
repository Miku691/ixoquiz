app.controller("categoryController", ['$scope', '$rootScope',
    function ($scope, $rootScope,
    ) {
        const init = () => {
            //variable 
        }
        $scope.ixoMsgCategory = "This is ixoquiz";
        
        init();
    }
]);